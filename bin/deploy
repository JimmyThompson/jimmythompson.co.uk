#!/bin/bash
set -euo pipefail

USER=$1
DESTINATION=$2

scp -o StrictHostKeyChecking=no -r _site ${USER}@${DESTINATION}:~

ssh -o StrictHostKeyChecking=no ${USER}@${DESTINATION} bash << EOF
    rm -rf jimmythompson.co.uk
    mv _site jimmythompson.co.uk
EOF
