#!/bin/bash
set -euo pipefail

USER=$1
DESTINATION=$2

rm -rf _site
bundle exec jekyll build

scp -r _site ${USER}@${DESTINATION}:~

ssh ${USER}@${DESTINATION} bash << EOF
    rm -rf jimmythompson.co.uk
    mv _site jimmythompson.co.uk
EOF
